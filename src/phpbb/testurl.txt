jQuery('.postbody').each(function () {
	var that = jQuery(this);
    var text = that.text();
	var pattern = /(\[iris\=).{24}(\]\[\/iris\])/;
	var matches = text.match(pattern);
	if ($.isArray(matches)) {
		console.log(matches[0])
    text = text.replace("lollypops", "marshmellows");
    $(this).text(text);
	}
})